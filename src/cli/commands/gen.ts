import "source-map-support/register.js"
import * as OC from "@oclif/core"

export class Command extends OC.Command {
  static override description =
    "Generate source code for apps, suites, and app and suite features"
  async run() {}
}
